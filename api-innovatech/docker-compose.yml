version: '3.9'

services:
  api:
    build: .
    container_name: api-innovatech
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
      PORT: 3000
    restart: unless-stopped
