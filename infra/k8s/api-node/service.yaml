apiVersion: v1
kind: Service
metadata:
  name: api-node
  namespace: innovatech
  labels:
    app: api-node
    tier: backend
spec:
  selector:
    app: api-node
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
